import { atom } from 'nanostores';

export const $siteFilter = atom<string | undefined>(undefined);
export const $yearFilter = atom<string | undefined>(undefined);
export const $wingFilter = atom<string | undefined>(undefined);
